.page-header
  h1
    | New album
    - if @albumable
      small
        |  for #{@albumable.class.name.downcase} ##{@albumable.id}

.bs-callout.bs-callout-info
  = render 'form'
  = link_to 'Cancel', albums_path, class: 'btn btn-cancel'
