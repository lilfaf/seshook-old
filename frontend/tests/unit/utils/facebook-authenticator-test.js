/* jshint expr:true */
import { expect } from 'chai';
import {
  describe,
  it
} from 'mocha';
import FacebookAuthenticator from 'frontend/utils/facebook-authenticator';

describe('FacebookAuthenticator', function() {
  // Replace this with your real tests.
  it('works', function() {
    var result = new FacebookAuthenticator();
    expect(result).to.be.ok;
  });
});
